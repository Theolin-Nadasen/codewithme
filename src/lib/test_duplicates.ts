import 'dotenv/config';
import { addProject } from '../actions/projects';

console.log("Duplicate name check added to src/actions/projects.ts");
console.log("Please verify manually by trying to add a project with the same name twice.");
